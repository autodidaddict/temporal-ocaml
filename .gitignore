_build/
rust/temporal_bridge/target/
dist/
*.install
*.merlin
.DS_Store
# dune now builds these under _build (see lib/dune). Ignored here only to guard
# against stale copies left in the source tree by the old staging script.
lib/libtemporal_core_bridge.a
lib/temporal_bridge.h
# Cargo.lock IS committed (reproducible builds).
